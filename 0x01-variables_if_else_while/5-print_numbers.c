#include <stdio.h>
/**
 * main - print numbers from 0 to 9
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	int y;

	for (y = 0; y < 10; y++)
	{
		printf("%d", y);
	}
	putchar('\n');
	return (0);
}

